%section
  %h2 Broken
  %img.kitten(src="#{Kitten.url(:broken)}")
  - @broken_projects.each do |project|
    .box.failure
      = project.name
      .status
        = "Broken for #{project.builds_since_last_success} build#{project.builds_since_last_success != 1 ? 's' : ''}"
