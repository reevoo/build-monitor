%section
  %h2 Pull Requests
  - @pull_requests.each do |project, requests|
    .project-requests
      %h3=project
      %ul
        - requests.each do |request|
          %li
            %img{src: request.user_avatar_url, width: 80, height: 80}
            %p=request.title
